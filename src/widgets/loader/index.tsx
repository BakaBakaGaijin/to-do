/* APPLICATION */
import Overlay from "../../shared/overlay";
import "./index.css";

const Loader = () => {
  return (
    <Overlay>
      <div className="loader">Loading...</div>
    </Overlay>
  );
};

export default Loader;
