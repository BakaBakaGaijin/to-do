/*


export default function addItem({description, id, name, categoryId}) {
    let item = {
        id, name, description, categoryId
    };
    db.put(todo, function callback(err, result) {
        if (!err) {
            console.log('Successfully posted a item!');
        }
    })
}*/
